- **POST Status Codes:** By default, explicitly set all `@Post()` endpoints to return 200 OK using `@HttpCode(HttpStatus.OK)`. Only use 201 if I explicitly state that the route creates a new resource.
- **Preserve Debugging Code:** NEVER remove, modify, or comment out any existing `console.log()` statements when updating or refactoring code. Strictly preserve them as they are crucial for my debugging process.